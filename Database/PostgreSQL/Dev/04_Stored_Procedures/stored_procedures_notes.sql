-- PostgreSQL — 04 Stored Procedures
-- Procedure creation
-- Syntax varies by dialect — see Comparisons/Stored_Procedures_Comparison.md

-- Parameters
-- IN, OUT, INOUT (Oracle/PLSQL); IN, OUT (SQL Server); IN only (PostgreSQL before v14)

-- Error handling
-- EXCEPTION blocks (Oracle/PLSQL), TRY/CATCH (SQL Server), EXCEPTION (PostgreSQL)

-- Transaction control
-- COMMIT, ROLLBACK inside procedures
