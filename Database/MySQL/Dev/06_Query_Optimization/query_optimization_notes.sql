-- MySQL — 06 Query Optimization
-- Execution plan
-- EXPLAIN / EXPLAIN ANALYZE (PostgreSQL)
-- EXPLAIN (MySQL), execution plan in SSMS (SQL Server)

-- Index strategy
-- B-tree for equality/range; covering indexes; avoid over-indexing

-- Query rewriting
-- Replace subqueries with JOINs; push predicates; avoid SELECT *

-- Statistics
-- ANALYZE (PostgreSQL), UPDATE STATISTICS (SQL Server), ANALYZE TABLE (MySQL)
